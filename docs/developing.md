# Developing

## On the Edgelab's Gaudi VM:

This repo provides a Docker development environment for Gaudi: `docker/gaudi.env.Dockerfile`. First, follow the
instructions there to build the Docker image locally. Then, wrap your call with `scripts/run_docker_dev.sh`, see usage
details there.
