## P0.5.1

From the [over-powered-decoder lever](phase0/P0.5.1.md), the decoder-capacity companion to [P0.5.0](phase0/P0.5.0.md)
(same mode, same instrument verdict; the shared framing lives in P0.5.0).

**Status: runs complete (from-scratch MAE, Imagenette, 5 seeds) — the decoder lever does not induce the mode either; the
clean full-rank vehicle delivers the sharpest blind-spot verdict. (Mode-status label is the user's call — see
index.md.)**

The over-powered decoder produces the full-rank geometry *cleanly* — RankMe stays high (shortcut rank equal-to-or-above
healthy) on every seed — and gives the apples-to-apples "loss lies" (both arms solve the same task, the decoder
compensates so the loss reads lower while the probe is flat). But the probe does not crater: the encoder stays useful,
so "full-rank but useless" achieved *full-rank* and not *useless*. This is the sharpest instrument verdict in P0.5:
`uniformity` mis-signals *consistently and strongly* — the shortcut looks more uniform (healthier) on every seed while
quality is unchanged — so it reads geometry, not quality; `alignment` is weakly right-direction but tiny; RankMe is
blind by construction. No reader fires. Producing genuine uselessness likely needs a competent rep with quality *to
lose* — the degrade-from-competent path, now pursued in [P0.5.2](phase0/P0.5.2.md) (a pretrained, capacity-gated MAE
against which the candidate MAE-native readers are put on trial).
