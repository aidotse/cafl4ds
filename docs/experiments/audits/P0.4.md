P0.4.0:

- Run with only one seed
- Contrast with only one other batch size
- Did not actually test that indicator is mute under AdamW -> worth a quick run
- No empirical validation of lead time: "Lead time is not free — it is inversely coupled to the step-size overshoot (the
    operating envelope)"

P0.4.1:

- Does not clearly specify at what point the measurements are taken - immediately past the shock, or after the tailing
    recovery period?
- Reads points 1 and 4 are confusing. What is 'change_ratio'? 'surprise_ratio'? 'lr_sweep' (The intro states LR states
    constant)?
- Finding #1 : uses burst_kind=noise or burst_kind=data?
- lead time shrinks with severity - can be concluded based off a a couple of runs?
- "P0.4.1: *within* MAE, the loss must be *raw* (not `norm_pix`) for its magnitude to track surprise" : require
    validating experiment or no?
