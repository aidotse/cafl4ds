# Positive control — data-driven surprise / shock, the deployment-relevant instability (P0.4.1)

**What this substudy is.** The *data-driven* companion to [P0.4.0](P0.4.0.md). Where P0.4.0 forces divergence with a
pathological learning rate (the clean, controllable lever), P0.4.1 asks the deployment-relevant question: does the
gradient-norm instrument fire on data surprise — an out-of-distribution burst arriving mid-stream — and can it tell a
*transient, recoverable* shock from a *runaway* one? Same instrumented streaming loop and same grad-norm instrument as
P0.4.0; the forcing lever is now the data, not the learning rate.

The headline result is a sharpening of what the instrument actually reads: the grad norm reads reconstruction-error
*magnitude* (a *content* shock), not distributional novelty. A saturating (out-of-range) shock fires cleanly; an
in-range distribution shift — even one that destroys image structure — does not.

## Why toy scale is valid (with a caveat P0.4.0 did not have)

Like divergence, a surprise *spike* is a fast, local response (a large loss on a hard batch → a large gradient), so the
*mechanism* reproduces at toy scale. The caveat, flagged in the original design and borne out here, is that surprise
magnitude depends on how well the base distribution is modelled: an under-trained MAE finds everything surprising,
voiding the contrast. So the base is genuinely warmed to competence first (raw reconstruction MSE `0.215 → 0.058` over
~4 epochs) and the run is gated on confirming that descent before any burst is injected. What does *not* transfer from
toy scale is the *genuine deployment surprise pole* (a correlated distribution drift over an unending horizon,
interacting with the closed active-selection loop) — that is Phase-1, the same hand-off made for the forgetting
correlation question (P0.3.8).

## Why MAE — and why **raw MSE, not `norm_pix`** — is the vehicle

MAE + SGD is carried from P0.4.0 (unbounded loss, undamped optimizer). The new choice is turning `norm_pix_loss` off.
The default MAE (P0.1.0/P0.4.0) normalizes each target patch to unit variance before the MSE, which caps the per-patch
loss near 1.0 for *any* unpredictable content — a competent MAE already sits at ~0.97, so surprise has no headroom to
register in the loss *magnitude*, and the grad norm cannot read it. This is now confirmed with a preserved artifact
(`norm_pix_seed0`, seed 0): re-running the two-sided gate with `norm_pix_loss=true` leaves the base loss essentially
flat (`1.10 → 0.99` — no competence headroom, so the base-competent check itself fails), and the saturating
`burst_scale=4` shock — the very shock that fires `78×` under raw MSE — produces only a **1.47× cross-arm grad blip**,
far below the `2.0×` fire threshold, so the gate correctly does *not* fire. Surprise detection needs the loss magnitude
to track reconstruction error, so the vehicle uses raw reconstruction MSE. This is the direct analogue of P0.4.0's "the
loss must be unbounded" finding — one level deeper, *inside* MAE.

## Design — a burst spliced into a warmed base stream

A hand-built stream over the same tiny MAE (img 32): `warm_steps=300` of a base distribution (STL-10) to reach
competence, `burst_steps=8` of a shock, then `tail_steps=100` of the base again so a transient spike's *recovery* is
observable. Two arms from a seed-reset, bit-identical start, the base steps indexed identically across arms so only the
burst-window content differs:

| arm | burst window | prediction |
| -- | -- | -- |
| **shock** | the injected burst | grad norm spikes (change point); then recovers *or* runs away |
| **control** | the base (no injection) | grad norm stays in the base band (quiet reference) |

The gate reads the two-sided contrast at the same steps (shock burst vs. the control's *same* steps — not the shock's
own pre-burst band, since the base loss is still descending across the burst, so the counterfactual "no shock" reading
is the control at those exact steps). Flat LR (no scheduler) and grad-clip off, so the burst is the sole perturbation
and a blow-up is not masked.

The two verdicts are read on **different windows**, worth stating plainly since the prose otherwise blends "spikes then
recovers": the *fire* (surprise + change-point) is measured over the **burst window** (steps `[300, 308)`), and
*recovery* (transient vs. runaway) over the **tail** (post-burst); the within-arm base band is the pre-burst window
(after `init_skip`). The positive-control fire (Finding #1) is a `burst_kind=noise` saturating shock; the refutation
(the distributional-OOD non-fire) is `burst_kind=dataset`.

**The burst content is the experiment.** `burst_kind=noise` injects uniform-random images scaled by `burst_scale`: at
amplitude 1 they are in the trained `[0, 1]` range; above 1 they **saturate out of range** (a sensor-corruption-style
content shock). `burst_kind=dataset` injects an in-range distributional OOD (CIFAR-100, optionally grayscale or
phase-scrambled) — the *naive* surprise vehicle.

### Reads

- **The gate (two-sided change point).** The shock's burst-window peak grad norm ≥ `fire_ratio` × the control's
    same-step peak (fires) — a **cross-arm** ratio; the base is competent (warm loss descended); the burst is genuinely
    surprising (shock burst loss ≥ `surprise_ratio` × the control's same-step loss); and the control is flat (its own
    **within-arm** burst/base band, `within_arm_ratio`, < `control_quiet_ratio`). The two grad-norm ratios are distinct
    quantities that once shared a name: `fire_ratio` is *cross-arm* (shock vs. control at the same steps — this is the
    gate), `within_arm_ratio` is *within-arm* (an arm's own burst peak vs. its pre-burst base band — the control's
    change-point sanity check). The headline "78×" fire is the *cross-arm* `fire_ratio`.
- **Transient vs. runaway (severity ≠ permanence).** Reported: does the spike recover to the base band in the tail
    (transient) or run away (divergence)? The `burst_scale` **amplitude** is a data-shock severity dose-response at
    fixed `lr_op`.
- **Lead time on a runaway.** Where the shock triggers a divergence, the grad-norm blow-up vs. the loss NaN — the P0.4.0
    leading-indicator read, data-triggered.
- **The `lr_sweep` negative control.** The gate itself is read with LR **fixed at `lr_op`** (data is the sole lever);
    the `lr_sweep` is a separate *off-operating-point* negative control that deliberately raises LR to `{3, 10}` to
    confirm warm-phase LR-driven divergence (P0.4.0) fires **shock-independently**. It does not contradict the fixed-LR
    premise — it is a control *against* it.

## Findings

Base: STL-10, tiny MAE img 32, SGD momentum 0.9, raw MSE, warmup & clip off; seed 0, reproduced seed 1.

- **Two-sided gate: PASS** (both seeds). The base is competent (loss `0.215 → 0.058`). The saturating noise shock
    (`burst_scale=4`) fires: burst loss **≈30× the control's same steps**, and a grad-norm change point of **78× (seed
    0\) / 74× (seed 1)** the control's same-step peak, while the control's own within-arm burst/base band stays flat
    (`0.06×`). The spike then **recovers** to the base band in the tail — a large severity with **zero permanence** (the
    P0.3 severity ≠ permanence analogue).

- **The refutation — distributional OOD does not fire.** An in-range distributional burst produces reconstruction error
    *comparable to the base* and no separable grad-norm change point. The sharpest case — and the one preserved artifact
    (`dataset_scrambled`) — is **phase-scrambled CIFAR** (`surprise 0.54× / fire 1.16×`): the shock destroys all image
    structure, yet the burst loss actually *drops below* the base, because smooth phase-scrambled texture is *easier* to
    inpaint than natural images. That is stronger than "OOD is invisible": the "harder" (structure-destroyed) input is
    *easier* to reconstruct, so MAE reconstruction difficulty is **anti-correlated** with the intuitive notion of
    novelty here — the single most convincing evidence that the grad norm reads inpaintability, not novelty. The other
    rungs fall the same way (shock/control same-step contrast, seed 0): color CIFAR `1.18× / 1.97×`, grayscale CIFAR
    `1.04× /   2.27×`, in-range noise `0.80× / 1.51×` — all at or below the natural grad-norm variance floor (a healthy
    grad norm fluctuates ~2–4× on batch noise), and all re-runnable via the `data_b` / `burst_scale` knobs but not
    separately promoted (phase-scramble alone carries the qualitative claim). MAE reconstruction difficulty tracks local
    inpaintability, not distributional novelty, so the grad norm is *not* a distribution-shift detector for this vehicle
    — it reads a *content* shock (error magnitude), which an in-range shift does not produce.

- **Severity dose-response on the shock amplitude (fixed `lr_op=1`; the full ladder seeded across 0/1/2,
    `scale_dose_seeded/`).** Below threshold the fire ratio climbs monotonically and every seed recovers (transient):
    `scale 1` sits at the grad-norm variance floor (fire `0.9–2.6×`, no genuine fire) → `scale 2` fires (`11–25×`) →
    `scale 4` fires (`34–78×`) — all three seeds transient, so the "fires but recovers" band is itself seed-robust. The
    transient→runaway threshold on the *data* axis sits near `scale 6`, and past it the inverse lead-time/severity
    coupling — the data-triggered face of P0.4.0's operating envelope — is now **seed-stable across ~7 runaway rungs × 3
    seeds** (seeds 0/1/2), not the original two points on one seed. The **median lead falls monotonically** with
    amplitude: `scale 8` → 11 → `10` → 7 → `12` → 6 → `14` → 6 → `16` → 5 → `24` → 4 → `32` → 4 steps (per-seed spread ≤
    4 steps). Two sharper facts fall out. **(i) The lead floors at ~4 steps** — the earliest any arm goes non-finite is
    step 304 (4 after the burst onset at 300) even at a `32×` shock, so the warning window does *not* shrink to zero; it
    bottoms at the handful of compounding steps an overflow needs. **(ii) `scale 6` is a seed-fragile knife-edge** — 1/3
    seeds runs away (lead 70, a slow tail overflow at step 370) while 2/3 recover; right at threshold the lead is
    longest *and* the outcome is stochastic across seeds — exactly the operating-envelope shape (lead maximal at the
    edge, floored deep past it). NaN completing in the *base tail* (e.g. step 314 for `scale 8`, burst window
    `[300, 308)`) shows the shock **triggered** the runaway rather than diverging during the burst. The full seeded
    ladder (artifact `scale_dose_seeded/`; runaway fire ratios omitted — they run `10⁶`–`10¹⁹` and, per A3, are
    truncation-sensitive, so only the binary runaway/no-runaway is portable):

    | `burst_scale` | regime | fire ratio | recovers | lead — steps (seeds 0/1/2) |
    | -- | -- | -- | -- | -- |
    | 1 | no-fire (variance floor) | `0.9–2.6×` | 3/3 | — |
    | 2 | transient | `11–25×` | 3/3 | — |
    | 4 | transient | `34–78×` | 3/3 | — |
    | 6 | **knife-edge** | `88–189×` | 2/3 | 70 (seed 1 only) |
    | 8 | runaway | ≫ fire | 0/3 | 14 / 10 / 11 |
    | 10 | runaway | ≫ fire | 0/3 | 8 / 7 / 6 |
    | 12 | runaway | ≫ fire | 0/3 | 6 / 6 / 6 |
    | 14 | runaway | ≫ fire | 0/3 | 6 / 6 / 6 |
    | 16 | runaway | ≫ fire | 0/3 | 5 / 5 / 5 |
    | 24 | runaway | ≫ fire | 0/3 | 4 / 4 / 4 |
    | 32 | runaway | ≫ fire | 0/3 | 4 / 5 / 4 |

- **The `lr_sweep` negative control.** At `lr = 3, 10` both the shock *and* the control go non-finite at the
    **identical** step (8, then 5) — this is warm-phase, LR-driven divergence (P0.4.0), which fires long before the
    step-300 burst, so it is **shock-independent**. An in-range shock doesnt necessarily lower the divergence threshold;
    only a large out-of-range *content* shock (the amplitude ladder) triggers divergence at a base-stable LR.

## Insights

- **The instrument reads a content shock (error magnitude), not distribution shift.** The clean positive control is a
    saturating out-of-range corruption; an in-range distribution shift — even structure-destroying phase-scramble — is
    invisible to the grad norm because it does not raise the reconstruction error above the base. A live grad-norm
    monitor would catch sensor corruption / saturation / gross artifacts, **not** a subtle covariate drift. That bound
    is the substudy's point.
- **Two nested vehicle findings on the loss.** P0.4.0: the loss must be *unbounded* (MAE MSE, not SimSiam cosine) to
    diverge. P0.4.1: *within* MAE, the loss must be *raw* (not `norm_pix`) for its magnitude to track surprise. Both are
    "the vehicle changes what the instrument can see."
- **Severity ≠ permanence, and lead time shrinks with severity toward a floor — on the data axis.** A
    severe-but-recoverable shock (large spike, full recovery) and a runaway differ by *amplitude* at a fixed LR, with a
    real threshold between them. Past it, the warning the grad norm gives shrinks monotonically as the shock worsens —
    the data-driven face of P0.4.0's operating envelope — now **established across ~7 runaway rungs × 3 seeds**, not the
    original n=2. But the shrink is bounded: the lead **floors at ~4 steps**, the compounding time an overflow needs
    even from an extreme gradient. So the operating envelope has a *shape* — the lead is longest (and seed-stochastic)
    right at the threshold, and floored deep past it.

## Build

No new library code — P0.4.1 reuses the P0.4.0 grad-norm-instrumented `StreamingLoop` unchanged. The substudy is a new
harness + config:

1. **Harness** — `scripts/positive_control_data_surprise.py`: builds the base batches and the burst (uniform noise ×
    `burst_scale`, or the `data_b` distributional OOD), splices the `warm → burst → tail` stream, runs the shock +
    control arms across the `lr_sweep`, and evaluates the two-sided change-point gate + the recovery / lead-time reads.
1. **Config** — `configs/positive_control_data_surprise.yaml`: the vehicle (`ssl.norm_pix_loss=false`, `optim=sgd`, clip
    off), the stream shape (`warm/burst/tail_steps`), the burst knobs (`burst_kind`, `burst_scale`), `lr_op` /
    `lr_sweep`, and the gate thresholds.

## Artifacts

[`artifacts/P0.4.1/`](artifacts/P0.4.1/) — the **data-surprise** reads (slimmed to `{gate, ladder}`): `gate_seed0` +
`gate_seed1` (the passing two-sided change-point gate — saturating-noise shock, burst grad-norm `78× / 74×` the
control's same steps, ~30× loss, transient recovery), `dataset_scrambled` (the distributional-OOD **non-fire** —
phase-scrambled CIFAR at surprise `0.54×` / fire `1.2×`, the refutation), `scale_dose/dose.json` (the `burst_scale`
amplitude **severity dose-response** — transient→runaway threshold between scale 4 and 8, lead time `14 → 5`), and
`norm_pix_seed0` (the **`norm_pix` pole** — the same saturating shock gives only a **1.47×** grad blip, surprise
invisible; the second half of the vehicle finding).

## How to run

The whole read (gate + LR ladder) runs in one session (seconds–minutes on CPU):

```bash
uv run python scripts/positive_control_data_surprise.py            # the two-sided gate (noise shock, scale 4)
```

The data-shock **severity dose-response** (no-fire → transient → runaway), and the refuted distributional-OOD ladder:

```bash
uv run python scripts/positive_control_data_surprise.py 'burst_scale=1'   # in-range: no fire
uv run python scripts/positive_control_data_surprise.py 'burst_scale=8'   # over threshold: runaway (lead time)
uv run python scripts/positive_control_data_surprise.py burst_kind=dataset data_b=cifar100_scrambled  # non-fire
uv run python scripts/positive_control_data_surprise.py ssl.norm_pix_loss=true  # norm_pix pole: surprise invisible (1.47x)
```

The seeded lead-vs-severity densification (`scale_dose_seeded/`) sweeps the runaway band across seeds; each rung is one
run at `lr_op` only (`lr_sweep=[1.0]`, 3× cheaper), read off the `lr=1` ladder rung's `lead`:

```bash
uv run python scripts/positive_control_data_surprise.py 'burst_scale=10' 'seed=1' 'lr_sweep=[1.0]'  # one (scale, seed) rung
```

Read: the shock's burst-window grad-norm spike vs. the control's same steps (fire), whether it recovers in the tail
(transient) or runs away (the amplitude threshold + lead time), and — on the `dataset` bursts — that an in-range
distribution shift does *not* separate from the base.
