# Phase 0 — Instrument Calibration

This is the landing page for Phase 0: it tracks the sub-studies and their motivation.

**Goal (from the [project plan](../../project-plan/index.md#strategic-ordering--instrument--demonstrate-first)):** build
the streaming loop + the measurement apparatus (e.g. RankMe/effective rank, VICReg variance/covariance,
alignment/uniformity, representation drift, per-era probe, frozen-backbone baseline **B5**) and — the real work —
**calibrate** it using a simple dataset (like STL10, CIFAR, Imagenette, etc.) and models. The point is not to scale, but
vet signal trustworthiness at a toy scale.

## Design / Philosophy

We consider the following constructs:

- **Failure mode** — a way a representation goes bad (collapse, forgetting, divergence, shortcut-degradation, etc.).
- **Vehicle** — the (PC - positive control arm, healthy arm) pair that induces/withholds that mode: a method + lever +
    data triple.
- **Instrument** — (also: thermometer, reader, metric, etc.) a measurement meant to detect the mode (RankMe, alignment,
    BWT, grad-norm, probe).

Examining the cross product across these dimensions is non-sensical (i.e not all instruments are applicable on all
vehicles by construction, and each have a selective mapping to failure modes they can catch). Instead, we focus on a key
subset of the possible combinations; each such corner *calibrates* the respective instrument such that we can then use
it to detect the same effects in Phase 1, once we scale past toy models and datasets.

### Calibration

1. **Calibration is a two-sided, per-failure-mode test.** A metric earns trust only if it (a) **fires** under a
    **positive control** that deliberately induces its failure mode, *and* (b) **stays quiet** under a **healthy**
    baseline (no false alarm). Failure modes are distinct, and each mode (not metric!) needs its **own** positive
    control.

1. **Calibration is counted mode-first**. The bar is **≥1** instrument calibrated per *failure mode* (not per metric);
    more than one *independent* reader is robustness upside — the aspiration beyond the bar, not a pass/fail
    requirement.

1. **Calibration is vehicle-aware**. Instrument are calibrated on vehicles that cleanly induce their respective failure
    mode (even if that vehicle isn't the eventual backbone / deployment strategy.)

1. **Prioritization and the readiness verdict are vehicle/backbone-aware**. A calibrated instrument only matters in
    deployment if its mode is native to the deployed backbone.

## Exit Gate

Each *failure mode* has **at least one** instrument calibrated for reading it — its PC fires *and* a healthy baseline
stays quiet. (Additional *independent* readers are robustness upside, not required to pass.)

## Failure Modes

**The suite is organized by failure mode, each with its natural vehicle(s).** Phase 0 builds a *portfolio* of
failure-mode instruments: collapse is a **joint-embedding** pathology (point/redundancy collapse of a projector), so its
instruments are calibrated on SimSiam / Barlow Twins and read at a **projector / terminal-`BatchNorm(affine=False)`
surface**; instability is MAE-native (its unbounded reconstruction loss can actually diverge), so it uses MAE (+ SGD for
divergence); forgetting is a continual-learning mode vehicled on both MAE and joint-embedding models - the vehicles
differ because the *modes* differ, not because a Phase-1 backbone has been chosen a-priori or not. This specificity is
by design, not an oversight.

| Failure mode | Positive control (induces it) + healthy reference | Instruments calibrated | Summary |
| -- | -- | -- | -- |
| **Collapse** (joint-embedding) | Collapse-forced SimSiam (predictor / stop-grad off → *point* collapse) and whitening-ablated Barlow (→ *redundancy* collapse), each vs. its intact healthy twin | RankMe, off-diagonal covariance, per-dim variance, alignment / uniformity — read at the **projector** | ✅ **Two-sided calibration against *both* collapse sub-modes — the decisive lesson is to read the geometry suite at the *projector*, not the backbone, where RankMe is the reliable core and the detectors track even mild, incipient collapse.** → [Study summary](../latest-results/P0.2.md) |
| **Forgetting** (MAE) | Split-task MAE: train task A, then only task B (the probe on A must crater), vs. a matched **replay** twin that revisits A | Backward Transfer / Forgetting Measure / per-era probe (reliable, but labelled); representation drift + the label-free recon-loss gap (weak, confounded); offline audits (kNN cross-probe, recoverability, savings) | ✅\* **MAE largely *resists* forgetting; the two-sided BWT/FM instruments are seed-stable-calibrated (recommend 🟡 → ✅), but they need labels, the label-free reader is confounded, and every crater is recoverable — so the genuine/permanent pole is a Phase-1 risk** (audit: [`../audits/P0.3.md`](../audits/P0.3.md)). → [Study summary](../latest-results/P0.3.md) |
| **Forgetting** (joint-embedding) | Same split-task + **replay** design as Forgetting (MAE), but on a **joint-embedding** vehicle (SimSiam) from a trained-in task-A well under a far cross-domain phase B | BWT / FM / frozen probe (ground truth, labelled), **plus** the JE **projector** — two label-free readers: a task-A-canary drift (A1) and a past-data-free current-stream drift (C3); `rankme_proj` guards collapse-specificity | ✅\* **Switching to a joint-embedding vehicle turns forgetting on where MAE resists; the calibrated two-sided reader is representation *drift* — trustworthy with a saved task-A batch (A1) and even past-data-free at the projector (C3). The audit ([`../audits/P0.6.md`](../audits/P0.6.md)) corrects the "collapse geometry doesn't port" claim: only `uniformity_proj` mis-signals (flips, 3/5); `alignment_proj` co-separates 5/5 and clears a healthy-vs-healthy seed-noise null, but reads the far-shift *regime*, not forgetting *magnitude* — a candidate *independent* second reader (robustness upside, not needed to pass, since drift already meets the ≥1-instrument gate). Caveats: the crater is shallow/seed-fragile, the drift separation is partly structural to the A-only control, and the "MAE resists the far shift" premise is unrun ([P0.3.10](P0.3.10.md)).** → [Study summary](../latest-results/P0.6.md) |
| **Instability** (optimization - divergence) | Pathological LR → divergence (P0.4.0); an out-of-distribution data burst spliced into a warmed stream (P0.4.1) | Gradient norm | ✅ **The cleanest Phase-0 mode — the gradient norm fires two-sided, specifically, and ~5 steps *ahead* of the loss NaN — but only under an unbounded loss + undamped optimizer; under standard AdamW + normalized loss the early warning vanishes, so the ✅ ships with a design recommendation to Phase-1.** → [Study summary](../latest-results/P0.4.md) |
| **Representation degradation** (MAE, shortcut / stable-loss) | Two MAE arms split by one shortcut lever — low **mask ratio** (P0.5.0) or over-powered **decoder** (P0.5.1/P0.5.2) — vs. a healthy MAE: the shortcut probe must crater while its **loss stays healthy-looking** | The P0.2 suite on trial at the backbone (RankMe, alignment / uniformity, frozen linear / kNN probe as ground truth), with the reconstruction loss as the *anti-instrument* that must NOT separate the arms; plus the MAE-native candidate **`alignment` under a stronger augment** | ✅\* **On an MAE backbone representation *quality* cannot be read label-free from any internal signal — the loss lies, the collapse-geometry suite is a trap (it mis-signals), and the one candidate reader tracks the degrade-lever's machinery, not quality — so the ✅ rests on those two robust *negatives*** (final label the reviewer's; audit: [`../audits/P0.5.md`](../audits/P0.5.md)). → [Study summary](../latest-results/P0.5.md) |

## Sub-studies

| ID | Sub-study | What it establishes | Status |
| -- | -- | -- | -- |
| P0.1.0 | [Instrument & the streaming loop](P0.1.0.md) | Motivation: stand up the apparatus the rest of Phase 0 calibrates on — the streaming loop plus the health instruments — and show it runs end-to-end. The class-blocked STL-10 stream, both SSL backbones (`C`), both inits (`I`), and the B-floor knob, logging SSL loss and health side by side. | ✅ **Complete** (the loop runs end-to-end on both CPU *and* the Gaudi HPU, with the instruments logging live and responding to the single-pass B-floor diet) |
| P0.2.0 | [Positive control (collapse gate)](P0.2.0.md) | Motivation: calibration for the *collapse* failure mode only. A configuration known to collapse, to prove the instruments *catch* collapse. Derive a baseline: predictorless / stop-gradient-off SimSiam (one toggle) forces collapse; side-by-side vs. intact SimSiam, from-scratch. | ✅ **Complete** (a predictorless / stop-grad-off SimSiam forces point collapse and the suite catches it side-by-side vs. intact SimSiam — but RankMe does *not* separate at the toy single-pass horizon, leaving the upper reference to P0.2.1) |
| P0.2.1 | [Positive control (collapse gate) - RankMe calibration](P0.2.1.md) | Motivation: P0.2 could not tell a healthy representation from a collapsed one (RankMe did not separate the arms), so the collapse gate had no trustworthy upper reference. P0.2.1 sets out to prove whether RankMe can separate a genuinely-collapsed representation from a genuinely-healthy one by (a) finding a regime where intact SimSiam demonstrably does *not* collapse — a genuine healthy baseline. Then: (b) testing how far that baseline reaches toward the project's target regime (correlated, single-pass), stress-tested with an LR sweep (annealed and flat) to rule out learning rate and schedule as the missing lever. We also want to find the the cheapest *robust* reference in terms of training horizon. | ✅ **Complete** (a genuine *non*-collapsing healthy baseline found — IID × 40 epochs — where RankMe separates ~2.8×; LR and schedule ruled out as the missing lever, and the cheapest robust training horizon fixed as the reference) |
| P0.2.2 | [Positive control (collapse gate) - other collapse metric calibration](P0.2.2.md) | Motivation: P0.2.1 gives us a healthy baseline and calibration for RankMe, but not for the remaining collapse metrics. Target: find their operating envelope (still on dummy / STL10 data), i.e. where they activate and where they remain quiet. Essentially repeat of P0.2.1 with the full collapse metric set. | ✅ **Complete** (the operating envelope of the full collapse-metric set on STL-10 — the projector-surface pair (RankMe, uniformity) fires across regimes, while `offdiag_cov` is calibrated as a principled *null* under point collapse (quiet side only → its fire side is P0.2.3)) |
| P0.2.3 | [Positive control (collapse gate) - redundancy-collapse vehicle](P0.2.3.md) | Motivation: P0.2.2 calibrated `offdiag_cov` on the *quiet* side only — it is a principled null under SimSiam *point* collapse, but has never been shown to *fire when it should*. Add a second collapse vehicle (a decorrelation method — Barlow Twins / VICReg — with its whitening term ablated) whose trivial optimum is *redundancy/dimensional* collapse, to close the fire side of `offdiag_cov` and contrast the suite's response against a second collapse sub-mode. A new `[STD]` SSL method add. | ✅ **Complete** (a second collapse vehicle — Barlow Twins with its redundancy-reduction term ablated → *redundancy* collapse — closes the fire side of `offdiag_cov` (fires ~13–20× robustly) while the per-dim variance discriminator stays quiet at the projector) |
| P0.2.4 | [Positive control (collapse gate) - false-positive stress on the quiet side](P0.2.4.md) | Motivation: every P0.2.x run so far varies how the PC and healthy arms *separate* — the healthy arm is always a well-behaved SimSiam — so the suite's **false-positive rate** is untested. Here: push a *genuinely-healthy* representation into regimes that superficially resemble collapse (low-diversity data, heavy augmentation, deliberate undertraining) and confirm no instrument false-fires. Compare against both a (a) canonically-healthy and (b) matched forced-collapse arm. Confirms *projector* false fires are eval-set artifacts (instrument still discriminates in-regime) while *backbone* ones are genuine, mechanistically confirming "read at the projector only" recommendation. | ✅ **Complete** (a genuinely-healthy representation pushed into collapse-resembling regimes — low-diversity, heavy aug, undertraining — does *not* false-fire; the Option-B matched-collapse ablation shows *projector* apparent false fires are eval-set artifacts while *backbone* ones are genuine → "read at the projector only") |
| P0.2.5 | [Positive control (collapse gate) - partial-collapse dose-response](P0.2.5.md) | Motivation: every P0.2.x run calibrates against *maximally-forced* collapse — the instruments have been dipped in ice and boiling water but never checked for a scale in between, so sensitivity to *incipient/gradual* degradation (the Phase-1 early-warning case) is untested. A **falsification / go-no-go probe** (not a ✅-gate): build a controlled partial-collapse continuum via two mechanistically distinct knobs (loss blend + soft stop-gradient) and read whether the projector detectors track intermediate severity monotonically (C1), whether projector geometry predicts backbone probe accuracy (C2), and surface the probe-accuracy contrast as an independent "healthy" anchor (C4). | ✅ **Complete** (pre-registered **both monotone** outcome, once the severity axis is resolved: a coarse ladder *looked* like a sharp step, but the whole drop sat in its first unsampled interval — a low-severity refinement recovers a **smooth, monotone ramp** through genuinely intermediate states, knob- and seed-invariant (ρ ≈ −1.0). So the "step" was too-coarse spacing; the projector detectors **read the incipient band**, **C1 closed** and the instrument choice *strengthened*. C2 reassuring — the projector tracks the backbone; C4 confirms healthy is more useful downstream. Narrower **Phase-1** question: does *real-scale* collapse ramp as gradually?) |
| P0.3.0 | [Positive control (forgetting gate) — make forgetting real + the two-sided gate](P0.3.0.md) | Motivation: first calibration of the *forgetting* mode — the collapse PC says nothing about the forgetting detectors. The hard first step is constructing a forgetting positive control that is actually *real* (the P0.2.1-analogue "find the fair regime"), then reading which instruments fire. *It overshot the original fire-side-only scope:* the healthy replay reference is a single toggle away, so P0.3.0 also delivers the matched quiet baseline and a complete, **passing** two-sided gate on one distinct-task corner. | ✅ **Complete** (a *real* forgetting positive control on one distinct-task (vehicles/animals) corner plus — one toggle away — the matched replay healthy reference, giving a complete, passing two-sided gate; BWT/FM are the reliable readers) |
| P0.3.1 | [Positive control (forgetting gate) — the operating point + drift verdict](P0.3.1.md) | Motivation: P0.3.0's crater is *modest and near the toy-scale noise floor* and its drift metrics were only *inconclusively* demoted. P0.3.1 asks whether the signal can be pushed clear of the floor and whether cka/cosine drift can be re-promoted — mapping the forgetting operating point across capacity and erosion, and settling the drift verdict. | ✅ **Complete** (mapped the forgetting operating point across capacity and erosion, pushing the crater clear of the toy noise floor; cka/cosine drift only *inconclusively* demoted here — the verdict is settled later on a real fire, P0.3.4) |
| P0.3.2 | [Positive control (forgetting gate) — multi-seed robustness of the fire](P0.3.2.md) | Motivation: the P0.2.2 analogue. P0.3.0/P0.3.1 read the fire from a *single* seed, near the −0.05 threshold; confirm it is **seed-stable** (min-across-N) before trusting it — the fire must survive the weakest replicate. | ✅ **Complete** (*refuted* — under precise multi-seed measurement the toy-scale fire is a single-seed probe-noise artifact (0/5 seeds fire, task B under-learns so nothing overwrites A); the BWT/FM instruments themselves read ≈0 correctly) |
| P0.3.3 | [Positive control (forgetting gate) — the batch-size probe](P0.3.3.md) | Motivation: P0.3.2 shows toy STL-10 does not forget robustly (task B under-learns). Test the one untried mechanism lever — a **smaller training batch** (more B-only steps + gradient noise) — as a last attempt to induce forgetting at toy scale before escalating the dataset. | ✅ **Complete** (the last toy-scale lever fails — a smaller training batch does *not* rescue the fire, task-B learning (R11) stays invariant → escalate the dataset) |
| P0.3.4 | [Positive control (forgetting gate) — supervised upper-limit on CIFAR-100](P0.3.4.md) | Motivation: toy STL-10 is exhausted as a forgetting vehicle (P0.3.1–P0.3.3) and MAE never fired. Escalate the *data* to CIFAR-100, the canonical catastrophic-forgetting benchmark, to (a) confirm whether MAE forgets there and (b) obtain a guaranteed fire — in the supervised native regime if needed — as an **upper-limit sanity check** that the BWT/FM readers react correctly; re-examine the drift verdict on a real fire. | ✅ **Complete** (an *upper-limit* supervised check: MAE **resists** forgetting even on CIFAR-100 — the key architectural finding — and a guaranteed fire comes only from a pretrained ViT-B/16 fine-tuned supervised (PC fires, healthy holds, `cka_drift` rehabilitated). *Not* the instrument+MAE target → P0.3.6) |
| P0.3.5 | [Forgetting-fire audit — genuine? permanent?](P0.3.5.md) | Motivation: the P0.2.4 analogue, now meaningful once a real fire exists (P0.3.4) — but an *audit* of that fire, not a constructed quiet-side calibration. Ask whether a fired BWT over-reads along two *orthogonal* axes: a kNN cross-probe (genuine vs apparent — real degradation or linear-probe artifact) and a recoverability re-learn (permanent vs recoverable — granting it's real, does it heal). Key framing: *recoverable ≠ fake*. | ✅ **Complete** (the fire is *genuine* — the kNN cross-probe craters deeper, so the refit-linear gate is conservative — and *severity ≠ permanence*: the operating-point fire fully recovers via a matched-LR task-A re-learn. An *audit*, not a constructed calibration — low-value for the refit-BWT (immune by construction), but a real calibration of the label-free MAE readers stays open future work) |
| P0.3.6 | [Positive control (forgetting gate) — the SSL-MAE fire (label-free)](P0.3.6.md) | Motivation: P0.3.4's fire is *supervised*; the calibration cafl4ds actually needs is **instrument + MAE, label-free**. Reuse the pretrained-deep-well lever with a **pretrained MAE** backbone continuing MAE training (no labels) on a cross-distribution phase B, and test whether an MAE backbone can be driven to a genuine crater *and* whether the label-free monitors (`cka_drift`, MAE recon loss) — the only signals available in deployment — track it. Its pass is what flips the forgetting **mode** to ✅. | ✅ **Complete** (the label-free MAE fire — refuted as seed-stable; **seed 0**: cross-dataset Imagenette→CIFAR with matched Imagenette replay fires PC BWT −0.062, healthy holds −0.003, 18.5× contrast — but at a single firing LR flanked by non-fires, crater depth confounded by R00 variance; the **MAE recon gap** corroborates (weakly — a replay-exposure differential), `cka_drift` does *not* transfer from the supervised fire. **Audit correction (C1/D1):** the N=5 replication fires only **1/5** (min −0.010) — a lucky-seed draw; the deep-well D1 variant, once corrected for a warm-up confound (decoder-first, not full-freeze), fixes the control and shows well-depth is a partial lever (deep fires 3/5 vs shallow 1/5) but still misses min-across-N → forgetting **mode 🟡**, see [`../audits/P0.3.md`](../audits/P0.3.md)) |
| P0.3.7 | [Positive control (forgetting gate) — savings audit of the label-free fire](P0.3.7.md) | Motivation: the P0.3.5 analogue for the *label-free MAE* fire — a genuinely distinct experiment (in MAE the objective is decoupled from the probe readout, so "apparent forgetting" is more likely than in the supervised case). Use the **savings paradigm** — truly forgotten ⇒ relearning is as slow as from scratch — to test whether the gate-passing BWT crater is genuine loss or recoverable interference. | ✅ **Complete** (the savings test *could not be run* on this fire — the P0.3.6 crater is too shallow for a relearning-speed gap and the from-scratch pole is the wrong control, so the savings fraction is degenerate. What holds is a weak retention check: the forgotten encoder recovers task A like the held one, far above a from-scratch MAE — consistent with "MAE resists catastrophic forgetting," but near-tautological for a −0.062 crater. Whether a genuinely *deep* MAE crater is permanent or recoverable stays untested → P0.3.8 / Phase-1) |
| P0.3.8 | [Positive control (forgetting gate) — hard-disjoint-distribution vehicle](P0.3.8.md) | Motivation: P0.3.7 left the savings guard's *genuine* (savings≈0) pole unpopulated in the MAE regime, and scoped the resistance finding to short/near-IID/task-swap conditions. Try one cheap, in-regime *controlled vehicle* toward the genuine pole — a **hard-disjoint** phase-B distribution (structure-destroyed, e.g. phase-scrambled) that disrupts representation-level structure — and, as a nearly-free rider, a **cosmetic** grayscale shift to test whether the recon-loss reader *false-fires* on an output shift the representation shrugs off. | ✅ **Complete** (the hard-disjoint phase-scramble arm did *not* deepen the crater despite destroying spatial structure, and a *harder* task B that learns less → MAE's resistance strengthens on a structure-destroying shift. The genuine pole stays *untested*, not certified → Phase-1. The grayscale rider confirms a **recon-loss false positive** — recon gap ~10× the fire while probe BWT holds at baseline, a decoder-output shift not representation forgetting) |
| P0.3.9 | [Seed-stability + well-depth de-confound + the lever hunt to a seed-stable fire (audit C1/D1)](P0.3.9.md) | Motivation: the [P0.3 audit](../audits/P0.3.md) flagged the P0.3.6 fire — the result that flipped the mode to ✅ — as single-seed / single-LR (the P0.3.2-untrustworthy configuration) with crater depth confounded by R00 well-depth. Run the two mandated checks: an N=5 seed replication (C1) and a well-depth de-confound (D1); then hunt the last inch to a seed-stable two-sided fire. | ✅ **Complete** (the promoted fire is refuted but a lever hunt recovers a seed-stable 5/5 — recommend 🟡 → ✅ for the instruments. C1: only 1/5 seeds fire at the P0.3.6 point, min PC BWT −0.010 — a lucky-seed draw, seed 0 reproduces the −0.062; D1: the as-found full-freeze variant was **warming-confounded** (a spurious 0/5), the **corrected decoder-first** D1 settles a deep trained-in R00 ≈ 0.84 that holds 5/5 and shows **well-depth is a partial lever** — deep fires 3/5 vs shallow 1/5 — but misses min-across-N because the replay-preventable differential is depth-invariant; **the last inch is phase-B pressure**: doubling the phase-B dwell (`epochs_b` 20 → 40) at the fixed deep well fires a **seed-stable 5/5** (worst −0.065, healthy holds 5/5). **Caveat:** the deeper crater is still ~fully recoverable (savings ≈ 1.0 at −0.085) — severity, not permanence — so the genuine/permanent pole (C2) stays a Phase-1 risk) |
| P0.3.10 | [Positive control (forgetting gate) — the far-domain MAE forgetting test at a deep well](P0.3.10.md) | Motivation: a late P0.3 addendum closing a reviewer-facing gap — the whole "MAE resists forgetting" conclusion rests on **near-domain** phase-B shifts (CIFAR / scramble / grayscale); the arc never tried a genuinely **far** domain. The P0.6 JE arc built one (EuroSAT) and its MAE companion contrast resisted — but at a shallow well *eroded* in phase A (raw ImageNet-MAE probes 0.89, dropped to ~0.69 before phase B on 32px-upsampled inputs). Redo it fairly: a **deep, un-eroded** MAE well (`img_size=224` + decoder-first short phase A → R00 ≈ 0.85+) under the far **EuroSAT** shift, A-only control, N=5, reading the **two-sided** BWT. Answers the phenomenon question, not an instrument calibration. | 🔲 **Designed, awaiting runs** (no code change — a config redo of the P0.6.0 MAE arm with the eroded-well confound removed; design doc up for a fresh session to pick up) |
| P0.4.0 | [Positive control (instability gate) — LR-driven divergence](P0.4.0.md) | Motivation: two-sided calibration of the **gradient-norm** instrument on the *divergence* pole, using LR as the clean forcing lever — grad norm must blow up (with / ahead of the loss going non-finite) under a pathological LR and stay bounded on a genuinely-learning healthy MAE. Plus a leading-indicator read (does the grad-norm spike *precede* the loss NaN?) and a collapse-specificity control (grad norm must **not** fire on the P0.2 collapse pole). MAE vehicle: its unbounded MSE loss can actually diverge, where SimSiam's bounded cosine loss collapses instead. | ✅ **Complete** (the two-sided grad-norm gate passes: healthy stays finite & learns, PC diverges; dose-response threshold `lr=3`, monotone, and — the payoff — **5 steps of lead time** before the loss NaN; specific to divergence, batch-robust, reproduced across seeds. Vehicle refined to MAE **+ SGD** — AdamW's normalized step damps the runaway to a no-lead knife-edge, now preserved as `adamw_seed0`) |
| P0.4.1 | [Positive control (instability gate) — data surprise / shock](P0.4.1.md) | Motivation: the deployment-relevant driver — data *surprise* (OOD / shift bursts in the stream), not hyperparameters. Calibrate grad norm as a transient shock / change-point detector and separate a recoverable spike from a runaway (the P0.3 *severity ≠ permanence* analogue). Toy-scale caveat: surprise magnitude depends on base-distribution competence. Full correlated closed-loop dynamics → Phase-1. | ✅ **Complete** (the grad norm reads a *content* shock, **not** distribution shift: a saturating out-of-range noise burst fires two-sided then *recovers* (transient), and `burst_scale` is a fixed-LR severity dose-response with a transient→runaway threshold and lead time shrinking with severity. In-range distributional OOD — CIFAR / grayscale / **phase-scramble** — does *not* fire (recon difficulty ≠ novelty); LR-divergence is shock-independent. Vehicle: raw MSE, `norm_pix` off — the `norm_pix`-on counterfactual, a bare 1.47× blip, is preserved as `norm_pix_seed0`) |
| P0.5.0 | [Positive control (representation-health gate) — MAE shortcut degradation & the P0.2 suite on trial (mask-ratio vehicle)](P0.5.0.md) | Motivation: calibrate the failure mode MAE *actually* exhibits (the forgetting arc showed catastrophic forgetting is a borrowed frame MAE resists). The anchor — carries the shared framing, but its real deliverable is the **instrument verdict, not the lever**: with the rep driven full-rank-but-useless (RankMe blind *by construction* — the mode's precondition), does **alignment/uniformity** track the frozen probe's crater on an MAE backbone, or does the P0.2 suite have a quality blind spot? The **mask ratio** is the cheapest lever to produce the geometry (low ratio → interpolation shortcut → loss looks healthy, rep useless). From-scratch, multi-seed. | 🟡 **Runs complete** (5 seeds — the mask lever does *not* induce the mode, gate FAILS 5/5; the loss lies within-arm but no reader tracks the crater (uniformity is noise). Nuance: mask partially erodes rank → P0.5.1 is the cleaner full-rank vehicle. Weak-baseline caveat + degrade-from-competent crater → P0.5 audit) |
| P0.5.1 | [Positive control (representation-health gate) — the over-powered-decoder lever](P0.5.1.md) | Motivation: the decoder-capacity companion to [P0.5.0](P0.5.0.md) (same mode, same instrument verdict). Drives the *same* degradation from the other side — an over-powered decoder does the semantic work so the encoder emits **full-rank but useless** features while the loss stays low — the lever built to stress the crux hardest (the decisive RankMe-misses test) *and* the **cleaner "loss lies"** (both arms at mask 0.75 → the same task → an apples-to-apples cross-arm loss, unlike P0.5.0). Orthogonal knob to P0.5.0 → independent evidence. | 🟡 **Runs complete** (5 seeds — the decoder lever does *not* induce the mode either, gate FAILS 5/5; clean full-rank precondition every seed + apples-to-apples 'loss lies'. Sharpest instrument verdict: uniformity mis-signals consistently, reading geometry not quality → the P0.2 suite has a quality blind spot on MAE) |
| P0.5.2 | [Positive control (representation-health gate) — the degrade-from-competent crater & the candidate MAE-native readers](P0.5.2.md) | Motivation: [P0.5.0](P0.5.0.md)/[P0.5.1](P0.5.1.md) confirmed the P0.2 suite's quality blind spot on MAE but could not *induce* the mode from-scratch at toy scale — and the readers meant to fill that gap need a genuine probe crater to calibrate against. Take the degrade-*from-competent* path: a **pretrained** MAE (real quality to lose) under a shortcut regime, **capacity-gated** (smaller competent model → less slack → the shortcut bites; controls MAE's stickiness confound, the deployment-shaped inverse the former P0.5.5 posed). Against that crater, put the candidate MAE-native quality readers on trial — **unsupervised clusterability** (silhouette / k-means on the frozen rep; the cheapest, most direct label-free proxy for the probe's separability), **mean attention distance** (shortcut encoders attend locally / copy neighbours, semantic ones grow long-range heads), and **alignment under stronger augmentations** (the invariance a semantic rep should have but a low-level-shortcut rep should not). Ground truth = frozen linear + kNN probe. Multi-seed. Absorbs the former P0.5.2–P0.5.5 idea-slots. | ✅\* **Complete** (5 seeds — the deliverable is POSITIVE: `alignment` under a stronger augment is a calibrated label-free reader for a genuine, full-rank MAE degradation; the * is that the degradation is shallow). Success was *reframed* to a real *full-rank* degradation (RankMe preserved), not a near-chance crater. The wells pretrain competent (clearing the P0.5.0/P0.5.1 wall); **degrade-from-competent resists** (MAE stickiness, the P0.3 lesson), so the degradation is induced **from-scratch at the now-competent scale** via the over-powered-**decoder** lever — the only lever that degrades the probe at preserved rank (the mask lever is too gentle; the combined lever and a shrunk encoder — the **capacity gate, refuted** — deepen the crater only by *collapsing* rank into P0.2 territory). Across 5 seeds the dissociation is seed-robust (RankMe preserved + loss *lower* on the bad arm every seed, probe worse every seed) and **`alignment_strong` fires two-sided 5/5** above a healthy/healthy null, where the whole P0.2 suite is blind or *mis-signals* (`uniformity`/`clusterability` say the degraded rep looks healthier — geometry, not quality). Caveats: the degradation is **shallow / magnitude-variable** (near probe-noise on some seeds), and `alignment_strong` reads the shortcut *regime* not the quality *magnitude* → Phase-1. (✅\* provisional — the instrument is calibrated seed-robustly; the shallow phenomenon + magnitude-tracking are Phase-1; final label is the reviewer's.) |
| P0.5.3 | [Positive control (representation-health gate) — the quality-vs-lever test for `alignment_strong` (decoder dose-down)](P0.5.3.md) | Motivation: [P0.5.2](P0.5.2.md) left the candidate reader `alignment_strong` calibrated only single-vehicle / single-point — it fires on the over-powered-**decoder** lever but stays at the null on the full-rank *mask* crater, so it cannot separate "reads **quality**" (the crater — deployable) from "reads the **decoder-lever geometry**" (Phase-1-absent). The audit's C1, settled by a **decoder dose-*down*** (seed 0): weaken the shortcut decoder from the firing 512×8 toward the healthy 64×2 and trace `alignment_strong` against the kNN crater (shrinking the decoder keeps the encoder full-rank, so it gives full-rank craters of decreasing depth). The verdict is the *shape* — fire declines with the crater ⇒ quality reader; fire flat-then-cliff while a crater persists ⇒ reads the decoder geometry (the likely result, per P0.5.2's flat-across-2.7× fire). A *light confirmation* of a strong existing lean, not a full study. | 🟡 **Runs complete** (seed-0 dose-down — `alignment_strong` refuted as a quality reader: it reads the over-powered-decoder *regime*, not quality). It fires *flat* across the whole decoder axis (+0.055 → +0.078) even where the shortcut probe *beats* healthy (crater −0.026), and stays silent on a real +0.042 mask-lever crater — a confounder, coupled to decoder strength (RankMe frac tracks it) and decoupled from quality. So the P0.5 ✅ re-anchors on its two robust negatives. No engineering — the decoder lever was a CLI override on the P0.5.2 harness. |
| P0.6.0 | [Positive control (forgetting gate) — the JE forgetting vehicle & the label-free projector *canary* reader (A1)](P0.6.0.md) | Motivation: the P0.3 arc showed MAE resists forgetting and leaves no trustworthy label-free reader on an SSL stream (C3/A1). The **anchor** of the P0.6 pair — carries the shared framing (the JE vehicle, the collapse trap, the ground truth) and targets **A1**: test a **joint-embedding** vehicle (SimSiam) on the proven P0.3.6/P0.3.9 split-task design — does it forget more readily, and does its **projector** surface (where P0.2 calibrated the suite) read on a **retained task-A canary** furnish a *trustworthy label-free* forgetting reader, cleaner than MAE's confounded recon gap? Still canary-bound (C3 → [P0.6.1](P0.6.1.md)). Deliverable = instrument verdict (frozen-probe BWT ground truth; do `alignment_proj` / `uniformity_proj` track the crater two-sided?), with `rankme_proj` as the collapse-specificity precondition. Trained-in task-A well, multi-seed. | ✅\* **Complete** (a *qualified* A1 success, final label reviewer's). The JE vehicle forgets where MAE resists, but only under a genuinely **far** phase-B shift (near-domain CIFAR does not clear the fire bar — a near-miss) and the crater is shallow/seed-fragile (4/5, `epochs_b=60` deepen only *moves* the lone miss). The A1 deliverable **reorders**: the *primary* hypothesis — the P0.2 projector-geometry suite reads forgetting — only partly holds. `uniformity_proj` is **refuted** (calibrated to *collapse*, it mis-signals on a forgetting crater, flipping direction, separates 3/5); but `alignment_proj` co-separates **5/5** and clears a healthy-vs-healthy seed-noise null — a candidate *regime-level* second reader (reads the far-shift regime, not forgetting magnitude), robustness upside beyond the gate, not a uniform negative. Representation **drift** on the task-A canary is the *trustworthy label-free* reader that carries the mode, tracking the crater two-sided **5/5** (`rankme_proj` full-rank throughout → forgetting, not collapse). A vehicle contrast swapping the objective to MAE **suggests** (not isolates — well-depth + init are confounded) that MAE resists the same far shift (drift ~25× lower leads; two-sided BWT ~10× smaller, cited as sign-consistency) — masked reconstruction anchors, JE churns; the clean deep-well far-domain MAE test [P0.3.10](P0.3.10.md) is unrun, so that leg is provisional. Carry-forwards: the far-domain MAE arm ran at a shallow phase-A-eroded well → the rigorous deep-well test is [P0.3.10](P0.3.10.md); the drift reader stays canary-bound → its past-data-free (C3) form is [P0.6.1](P0.6.1.md) |
| P0.6.1 | [Positive control (forgetting gate) — the *past-data-free* drift reader (C3)](P0.6.1.md) | Motivation: [P0.6.0](P0.6.0.md) closes A1 but stays canary-bound; the **companion** targets **C3** — a reader that keeps **no task-A data at all**. Same JE vehicle and config, **re-run** with `current_stream_drift=true` (HPU non-determinism draws 5/5 this wave vs P0.6.0's 4/5 — within noise): representation **drift** (`cka_drift` / `cosine_drift`, both surfaces) referenced to a fixed post-A snapshot of a **current-stream (phase-B)** batch, so it needs no retained task-A data — a deployable rolling monitor. The honest limitation: drift reads representation *change*, not forgetting (the healthy arm also moves), so the two-sided control is what certifies a forgetting-specific signal; disambiguating drift *without* it is Phase-1. Gated on P0.6.0, multi-seed. | ✅\* **Complete** (a *qualified* C3 success, final label reviewer's). N=5 on the P0.6.0 vehicle + `current_stream_drift`; the crater is present 5/5 (`rankme_proj` full-rank throughout → forgetting, not collapse). **A past-data-free reader exists:** coordinate-frame drift at the projector (`cosine_drift_proj`) separates the forgetting arm from the matched A-only arm two-sided **5/5** — the first forgetting reader that keeps *no* task-A data. **But dropping the canary costs a surface + a metric:** the backbone content-drift (`cka_drift`) that was the seed-stable *A1* winner on the task-A canary does **not** port (3/5, *inverts* on a seed) — pinned to a far-domain phase-B reference, the healthy arm's backbone churns on the unfamiliar input too, so the separation collapses; only the projector's frame-churn stays forgetting-specific. Calibration only: on this vehicle part of the PC drift is benign "learning B" plasticity, so the two-sided control is load-bearing and deployment disambiguation (no control to subtract) is Phase-1 |

## Handover to Phase 1

The TODOs the sub-studies explicitly promoted to Phase 1 — calibration is not operational readiness. The recurring
theme, stated once: every threshold is tuned at **toy scale** (tiny ViT, 32px, small `proj_dim`), so its transfer to a
real backbone / embedding dim is a live Phase-1 scaling question throughout.

| Mode | Source | Phase-1 TODO |
| -- | -- | -- |
| **Loop** | P0.1.0 | Interpret instrument *movement* (RankMe drift, rep-drift climb under a correlated single-pass diet) as degradation vs. benign reorganization, read against the P0.2–P0.4 gates. |
| **Collapse** | P0.2.1 | Carry the fixed **IID × 40ep** healthy reference — it does *not* transfer to correlated / single-pass corners; read single-pass RankMe against it, not a live "healthy" arm. |
| **Collapse** | P0.2.1 | Quantify correlation-over-horizon: ordering penalty is a multi-epoch effect, so single-pass degradation may be milder than the 40ep ablation implies. |
| **Collapse** | P0.2.1 | Expect the U-shaped healthy signature — don't misread the early RankMe dip (healthy reorganization) as a fire. |
| **Collapse** | P0.2.2/P0.2.5 | Determine whether *real-scale* collapse degrades as gradually as the toy vehicle. P0.2.5 (with a low-severity refinement) showed the projector detectors read a **smooth, monotone incipient ramp** at toy scale — the apparent "step" was too-coarse severity spacing — so early-warning sensitivity is calibrated *in shape*; only the toy-scale thresholds and the real-scale graduality remain to confirm. |
| **Collapse** | P0.2.4 | Apply the matched-class era-eval fix in the live loop — built and confirmed at toy scale (`EvalSets.restrict_to_classes`); Phase-1 era sets are few-class, so each era-comparison RankMe read must run against a class-matched reference. |
| **Collapse** | P0.2.2/P0.2.4 | Gate on the projector surface; keep the backbone only as reference (genuine in-regime blind spots). |
| **Forgetting** | P0.3.4/P0.3.7/P0.3.8/P0.3.9 | The central question: does correlation × long horizon break MAE's forgetting resistance? The genuine (deep, permanent, savings ≈ 0) MAE pole is unreached in-regime — untested, not absent — and [P0.3.9](P0.3.9.md) shows it is **not manufacturable by a cheap toy lever alone**: a deep, *trained-in* consolidated well (R00 ≈ 0.84, decoder-first) deepens the fire (3/5 vs a shallow well's 1/5) and a longer phase-B dwell (`epochs_b=40`) makes it **seed-stable 5/5** — but even that deepest crater (−0.085) stays ~fully recoverable (savings ≈ 1.0, bottom 0.742 ≫ the ~0.40 floor), i.e. severity, not permanence. So neither a deeper toy well nor a doubled toy horizon reaches the permanent pole; it genuinely needs the real-scale **correlation** regime. |
| **Forgetting** | P0.3.1 | A materially deeper crater needs richer data / a larger model — toy STL-10 is bounded. |
| **Forgetting** | P0.3.5 | Carry the recoverability check into deployment (replay budget / kNN cross-read) — a fire is "degradation now", not permanence, and a permanence knee exists up-gradient. |
| **Forgetting** | P0.3.5/P0.3.7 | Calibrate the label-free savings-to-criterion reader — no toy fire is deep enough, and a valid savings ≈ 0 reference can't be built at toy scale. **Anchor (D3):** the from-scratch MAE floor is ~0.38–0.40 on 10-way task A (random-init ViT-B probes ~0.38, from-scratch MAE plateaus ~0.40), so MAE task-A accuracies live on `[0.38, 0.70]` — any crater near ~0.40 is catastrophic, while the current fires (bottom 0.645) are nowhere near it. |
| **Forgetting** | P0.3.8 | Carry the two-sided replay control — the recon-loss reader false-fires ~10× on cosmetic output shifts (grayscale) that leave the representation intact. |
| **Forgetting** | P0.3.9 | The promoted P0.3.6 fire does **not** replicate ([P0.3.9](P0.3.9.md), C1: 1/5 seeds — a lucky-seed draw, seed 0 reproduces). The deep-well D1 lever, corrected for a warm-up confound (decoder-first), fixes the two-sided control (healthy holds 5/5) and lifts the fire to 3/5 — **well-depth is a partial lever** — but misses min-across-N because the replay-preventable differential is depth-invariant. **The last inch is phase-B pressure, not more well:** doubling the phase-B dwell (`epochs_b` 20 → 40) at the fixed deep well fires a **seed-stable 5/5** (worst −0.065, healthy holds 5/5), so the two-sided instruments now have a seed-stable positive control (**recommend 🟡 → ✅**). The standing caveat → Phase-1: the deeper crater is still ~fully recoverable (savings ≈ 1.0 at −0.085) — severity, not permanence — so the genuine/permanent pole needs the real-scale **correlation** regime, not more toy horizon. |
| **Forgetting** (joint-embedding) | P0.6.0 | Carry **drift** as the primary reader: canary drift tracks the crater two-sided 5/5 (and links to crater depth at the deep well), so a JE deployment reads forgetting via drift, not the reused collapse geometry. The projector-geometry suite splits — `uniformity_proj` mis-signals (*flips direction*, separates 3/5), but `alignment_proj` co-separates 5/5 and clears a seed-noise null as a candidate *regime-level* second reader (reads the far-shift regime, not forgetting magnitude) — robustness upside, not the primary. Drift's forgetting-specificity rests on the two-sided control (the healthy arm also drifts), and the crater is shallow/seed-fragile at the ~±0.04 noise floor — so magnitude/permanence, and whether a *real-scale* JE forgets at all, are open. **Anchor (D3):** the **JE forgetting-onset distance** — the shift distance where a JE representation stops absorbing and starts overwriting (SimSiam resists near CIFAR, forgets far EuroSAT) — is the JE analogue of the P0.3 from-scratch floor, a reference Phase-1 can calibrate real-scale shift-distance against. |
| **Forgetting** (joint-embedding) | P0.6.1 | A **past-data-free** drift reader exists but only as projector coordinate-frame churn (`cosine_drift_proj`, 5/5); the backbone content-drift that carried A1 does **not** port (3/5, *inverts* on a seed) because a far-domain current-stream reference churns both arms' backbones — so a rolling JE monitor must read the projector's frame-churn specifically. Disambiguating a live current-stream drift spike **without** the two-sided control — the same P0.1.0 handover problem — is the deployment gap. |
| **Instability** | P0.4.0/P0.4.1 | Both poles now mapped at toy scale (`adamw_seed0` / `norm_pix_seed0`): grad-norm early-warning (leading indicator + surprise change-point) exists **only** under raw MSE + SGD and vanishes under AdamW + `norm_pix`. Phase-1 decision: choose the optimizer/loss with this tradeoff in view; if AdamW + `norm_pix` is adopted, an early-warning instability signal is an open problem. |
| **Instability** | P0.4.1 | The genuine deployment surprise pole (correlated drift × unending horizon × closed selection loop) does not transfer from toy scale. |
| **Representation degradation** | P0.5.2/P0.5.3 | The one label-free candidate — `alignment` under a stronger augment — is **not** a quality reader: the [P0.5.3](P0.5.3.md) decoder dose-down shows it tracks the over-powered-decoder *regime* (it fires with *no* quality loss and stays silent on a real mask-lever crater), a confounder Phase-1 will not carry. So an MAE backbone has **no internal label-free quality reader**; whether a competent real-scale MAE has a *deeper* / **permanent** full-rank degradation, and whether *any* label-free reader can track it, are the open scaling questions. |
| **Representation degradation** | P0.5.0/P0.5.1/P0.5.2/P0.5.3 | On an MAE backbone, representation **quality is not readable label-free from any internal signal**: the P0.2 geometry suite mis-signals (`uniformity` / `clusterability` wrong-direction — the degraded rep looks *healthier*; RankMe blind by construction, kept only as the full-rank precondition / collapse guard), and `alignment` under a stronger augment reads the over-powered-decoder *regime*, not quality ([P0.5.3](P0.5.3.md)). So label-free quality monitoring needs a **labelled canary**, or a backbone whose geometry tracks quality (a **joint-embedding projector** — [P0.6.0](P0.6.0.md)); MAE's internals cover only the *loud* failures (divergence via grad-norm, rank collapse via RankMe). |

## Artifacts

Each `scripts/positive_control.py` run writes a full `comparison.json` — `{gate, envelope, pc, healthy}`: the gate
verdict, the collapse-suite envelope, and both arms' full per-checkpoint health series — into the **Hydra run dir**
(`outputs/<date>/<time>/`, **gitignored and ephemeral**), so fresh runs are never tracked. The forgetting harness
`scripts/positive_control_forgetting.py` (P0.3) writes an analogous `comparison.json` = `{gate, pc, healthy}` (no
collapse envelope; each arm is a single two-phase summary record — accuracy matrix, BWT/FM, drift, recon). The
instability harness `scripts/positive_control_instability.py` (P0.4.0) writes `comparison.json` =
`{gate, lead_time, healthy, sweep}` — the gate verdict (two-sided contrast + dose-response + divergence), the lead-time
block, and per-arm summaries (steady grad-norm peak/median, first non-finite step, init/final loss); the bulky per-step
`{step, loss, grad_norm, finite}` traces that feed the console sparklines are dropped from the tracked artifact. The
data-surprise harness `scripts/positive_control_data_surprise.py` (P0.4.1) writes `comparison.json` =
`{gate, ladder, op_traces}` — the two-sided change-point gate at `lr_op`, the per-LR `ladder` (shock vs. control
summaries + any lead time), and the `op_traces` per-step series; the traces are likewise dropped from the tracked
artifact. The shortcut harness `scripts/positive_control_shortcut.py` (P0.5) writes `comparison.json` =
`{gate, envelope, shortcut, healthy, well_baseline_{healthy,shortcut}}` — the mode-induction gate (the reframed
full-rank-first criteria: RankMe-frac precondition + a real seed-robust probe degradation + loss-does-not-alarm +
contrast, plus the reported reader verdict and, for the pretrained-continue vehicle, the P0.3.9 warm-up
recovery-to-baseline diagnostic), the P0.2-suite envelope, both arms' full per-checkpoint health series, and the
pristine pre-continue well baselines (pretrained vehicle only). The bulky arm series is dropped on promotion →
`{gate, envelope}`.

The runs that **back a substudy's cited numbers** are promoted, tracked, under `artifacts/<substudy-id>/`. The tracked
artifact is the **`{run, gate, envelope}` summary** — the gate verdict and the per-`(instrument × surface)` separations
that every table and post-hoc analysis actually reads, plus a `run` label identifying the config. The bulky
per-checkpoint `pc`/`healthy` series is dropped (it feeds only the console sparklines, which no table cites) to keep the
tree lean as substudies accumulate; re-run to regenerate it. Promoted artifacts are period-accurate — a file's gate
schema reflects the harness at the time of the run and is not retrofitted.

The **per-substudy listings** — which runs back each substudy's cited numbers, and the slim schema each was promoted to
— live in each substudy's detail doc, under its own `## Artifacts` section (`phase0/P0.X.Y.md`). Two substudies have no
directory of their own: P0.2.0 predates the writer, and P0.2.1 is re-backed in P0.2.2's `E1/` corner — see their docs.
