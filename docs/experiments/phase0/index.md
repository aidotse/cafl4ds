# Phase 0 — Instrument Calibration

This is the landing page for Phase 0: it tracks the sub-studies and their motivation.

**Goal (from the [project plan](../../project-plan/index.md#strategic-ordering--instrument--demonstrate-first)):** build
the streaming loop + the measurement apparatus (RankMe/effective rank, VICReg variance/covariance, alignment/uniformity,
representation drift, per-era probe, frozen-backbone baseline **B5**) and — the real work — **calibrate** it using a
simple dataset (STL10) and model. Phase 0 tests each health metric against the failure mode it is meant to catch, so
that a Phase-1 signal is interpretable once we scale past the toy model and dataset.

**Calibration is a two-sided, per-failure-mode test.** A metric earns trust only if it (a) **fires** under a **positive
control** that deliberately induces its failure mode, *and* (b) **stays quiet** under a **healthy** baseline (no false
alarm). The failure modes are distinct — a collapse run is *not* a known-forgetting event — so each mode (not metric!)
needs its **own** positive control. This is what drives the sub-studies below:

| Failure mode | Positive control (induces it) + healthy reference | Instruments calibrated | Status |
| -- | -- | -- | -- |
| **Collapse** (joint-embedding) | Predictorless / stop-grad-off SimSiam vs. a genuine healthy SimSiam baseline (point collapse); Barlow Twins with its redundancy-reduction term ablated vs. intact Barlow (redundancy collapse) | RankMe/effective rank, VICReg variance + off-diag covariance, alignment/uniformity, prototype entropy | ✅ Suite calibrated two-sided against **both** collapse sub-modes (point *and* redundancy). The trustworthy surface is the **projector**, not the backbone: the (RankMe, uniformity) pair fires across all regimes and `offdiag_cov` fires on redundancy collapse while per-dim variance stays flat, whereas backbone RankMe separates only in a narrow (IID, multi-epoch) regime and `offdiag_cov` at the backbone genuinely false-alarms on undertrained / low-diversity healthy arms → **read at the projector only**. |
| **Forgetting** (MAE) | Split-task run: train hard on era A, then only era B; the past-era probe on A must crater — with a matched **replay** control (phase B revisits A) that must hold | Per-era probe accuracy, Backward Transfer, Forgetting Measure (the reliable signal); representation drift (CKA/cosine) + the MAE reconstruction-loss retention gap (label-free corroborator — reported, not gated; over-fires on output-distribution shifts, P0.3.8); plus **offline audits** that qualify a fired crater — kNN cross-probe, recoverability re-learn, relearning-savings (offline, need extra data + retraining; P0.3.5/P0.3.7/P0.3.8) | ✅ **Calibrated in the target regime — a label-free SSL-MAE fire with a working label-free corroborator.** The trustworthy readers are BWT/FM (labelled) and the **MAE reconstruction-loss gap** (label-free); CKA drift does *not* transfer from the supervised fire, and the recon reader false-fires on output-distribution shifts. **Key architectural finding:** MAE *resists* catastrophic forgetting under short, near-IID, task-swap conditions — a fire came only from a pretrained deep well plus **matched-distribution replay**, and the crater is genuine *now* (the kNN cross-probe on the supervised fire, P0.3.5). Permanence, though, stays **untested** in the MAE regime: every MAE fire available is too shallow for the savings guard to certify (the from-scratch pole is the wrong control, and no deep crater formed even under a structure-destroying shift — P0.3.7/P0.3.8). So the *genuine* (permanent / savings≈0) pole is unreached; whether correlation × long horizon breaks the resistance is an open **Phase-1** question. |
| **Instability** (divergence) | Drive LR / batch pathology until training diverges | Gradient norm (minor mode) | ✅ **Both poles calibrated — LR-driven divergence and data-surprise.** The gradient-norm gate fires two-sided and *leads* the loss NaN (the lead-time payoff), is specific to divergence (collapse keeps it bounded), and reads reconstruction-error **magnitude** — a *content* shock, not distributional novelty — with a recoverable spike separable from a runaway (severity ≠ permanence). **Vehicle finding:** the signal needs an *unbounded* loss (MAE raw MSE) *and* an *undamped* optimizer (SGD) — AdamW's normalized step and per-patch `norm_pix` both erase it. Full correlated closed-loop gradient dynamics → **Phase-1**. |

**Exit gate:** every mode calibrated — its PC fires *and* a healthy baseline stays quiet.

## Sub-studies

| ID | Sub-study | What it establishes | Status |
| -- | -- | -- | -- |
| P0.1 | [Instrument & the streaming loop](P0.1.md) | The instruments (health metrics / measurements) can be queried, and respond to a single-pass diet. The loop + class-blocked STL-10 stream + both SSL backbones (`C`) + both inits (`I`) run end-to-end under the B-floor knob, logging SSL loss and health side by side — on both CPU and the Gaudi HPU. | ✅ **Complete** (the loop, the class-blocked STL-10 stream, both SSL backbones and both inits run end-to-end — CPU *and* Gaudi HPU — with the health instruments logging live and responding to the single-pass B-floor diet) |
| P0.2 | [Positive control (collapse gate)](P0.2.md) | Motivation: calibration for the *collapse* failure mode only. A configuration known to collapse, to prove the instruments *catch* collapse. Derive a baseline: predictorless / stop-gradient-off SimSiam (one toggle) forces collapse; side-by-side vs. intact SimSiam, from-scratch. | ✅ **Complete** (a predictorless / stop-grad-off SimSiam forces point collapse and the suite catches it side-by-side vs. intact SimSiam — but RankMe does *not* separate at the toy single-pass horizon, leaving the upper reference to P0.2.1) |
| P0.2.1 | [Positive control (collapse gate) - RankMe calibration](P0.2.1.md) | Motivation: P0.2 could not tell a healthy representation from a collapsed one (RankMe did not separate the arms), so the collapse gate had no trustworthy upper reference. P0.2.1 sets out to prove whether RankMe can separate a genuinely-collapsed representation from a genuinely-healthy one by (a) finding a regime where intact SimSiam demonstrably does *not* collapse — a genuine healthy baseline. Then: (b) testing how far that baseline reaches toward the project's target regime (correlated, single-pass), stress-tested with an LR sweep (annealed and flat) to rule out learning rate and schedule as the missing lever. We also want to find the the cheapest *robust* reference in terms of training horizon. | ✅ **Complete** (a genuine *non*-collapsing healthy baseline found — IID × 40 epochs — where RankMe separates ~2.8×; LR and schedule ruled out as the missing lever, and the cheapest robust training horizon fixed as the reference) |
| P0.2.2 | [Positive control (collapse gate) - other collapse metric calibration](P0.2.2.md) | Motivation: P0.2.1 gives us a healthy baseline and calibration for RankMe, but not for the remaining collapse metrics. Target: find their operating envelope (still on dummy / STL10 data), i.e. where they activate and where they remain quiet. Essentially repeat of P0.2.1 with the full collapse metric set. | ✅ **Complete** (the operating envelope of the full collapse-metric set on STL-10 — the projector-surface pair (RankMe, uniformity) fires across regimes, while `offdiag_cov` is calibrated as a principled *null* under point collapse (quiet side only → its fire side is P0.2.3)) |
| P0.2.3 | [Positive control (collapse gate) - redundancy-collapse vehicle](P0.2.3.md) | Motivation: P0.2.2 calibrated `offdiag_cov` on the *quiet* side only — it is a principled null under SimSiam *point* collapse, but has never been shown to *fire when it should*. Add a second collapse vehicle (a decorrelation method — Barlow Twins / VICReg — with its whitening term ablated) whose trivial optimum is *redundancy/dimensional* collapse, to close the fire side of `offdiag_cov` and contrast the suite's response against a second collapse sub-mode. A new `[STD]` SSL method add. | ✅ **Complete** (a second collapse vehicle — Barlow Twins with its redundancy-reduction term ablated → *redundancy* collapse — closes the fire side of `offdiag_cov` (fires ~13–20× robustly) while the per-dim variance discriminator stays quiet at the projector) |
| P0.2.4 | [Positive control (collapse gate) - false-positive stress on the quiet side](P0.2.4.md) | Motivation: every P0.2.x run so far varies how the PC and healthy arms *separate* — the healthy arm is always a well-behaved SimSiam — so the suite's **false-positive rate** is untested. Here: push a *genuinely-healthy* representation into regimes that superficially resemble collapse (low-diversity data, heavy augmentation, deliberate undertraining) and confirm no instrument false-fires. Compare against both a (a) canonically-healthy and (b) matched forced-collapse arm. Confirms *projector* false fires are eval-set artifacts (instrument still discriminates in-regime) while *backbone* ones are genuine, mechanistically confirming "read at the projector only" recommendation. | ✅ **Complete** (a genuinely-healthy representation pushed into collapse-resembling regimes — low-diversity, heavy aug, undertraining — does *not* false-fire; the Option-B matched-collapse ablation shows *projector* apparent false fires are eval-set artifacts while *backbone* ones are genuine → "read at the projector only") |
| P0.3.0 | [Positive control (forgetting gate) — make forgetting real + the two-sided gate](P0.3.0.md) | Motivation: first calibration of the *forgetting* mode — the collapse PC says nothing about the forgetting detectors. The hard first step is constructing a forgetting positive control that is actually *real* (the P0.2.1-analogue "find the fair regime"), then reading which instruments fire. *It overshot the original fire-side-only scope:* the healthy replay reference is a single toggle away, so P0.3.0 also delivers the matched quiet baseline and a complete, **passing** two-sided gate on one distinct-task corner. | ✅ **Complete** (a *real* forgetting positive control on one distinct-task (vehicles/animals) corner plus — one toggle away — the matched replay healthy reference, giving a complete, passing two-sided gate; BWT/FM are the reliable readers) |
| P0.3.1 | [Positive control (forgetting gate) — the operating point + drift verdict](P0.3.1.md) | Motivation: P0.3.0's crater is *modest and near the toy-scale noise floor* and its drift metrics were only *inconclusively* demoted. P0.3.1 asks whether the signal can be pushed clear of the floor and whether cka/cosine drift can be re-promoted — mapping the forgetting operating point across capacity and erosion, and settling the drift verdict. | ✅ **Complete** (mapped the forgetting operating point across capacity and erosion, pushing the crater clear of the toy noise floor; cka/cosine drift only *inconclusively* demoted here — the verdict is settled later on a real fire, P0.3.4) |
| P0.3.2 | [Positive control (forgetting gate) — multi-seed robustness of the fire](P0.3.2.md) | Motivation: the P0.2.2 analogue. P0.3.0/P0.3.1 read the fire from a *single* seed, near the −0.05 threshold; confirm it is **seed-stable** (min-across-N) before trusting it — the fire must survive the weakest replicate. | ✅ **Complete** (*refuted* — under precise multi-seed measurement the toy-scale fire is a single-seed probe-noise artifact (0/5 seeds fire, task B under-learns so nothing overwrites A); the BWT/FM instruments themselves read ≈0 correctly) |
| P0.3.3 | [Positive control (forgetting gate) — the batch-size probe](P0.3.3.md) | Motivation: P0.3.2 shows toy STL-10 does not forget robustly (task B under-learns). Test the one untried mechanism lever — a **smaller training batch** (more B-only steps + gradient noise) — as a last attempt to induce forgetting at toy scale before escalating the dataset. | ✅ **Complete** (the last toy-scale lever fails — a smaller training batch does *not* rescue the fire, task-B learning (R11) stays invariant → escalate the dataset) |
| P0.3.4 | [Positive control (forgetting gate) — supervised upper-limit on CIFAR-100](P0.3.4.md) | Motivation: toy STL-10 is exhausted as a forgetting vehicle (P0.3.1–P0.3.3) and MAE never fired. Escalate the *data* to CIFAR-100, the canonical catastrophic-forgetting benchmark, to (a) confirm whether MAE forgets there and (b) obtain a guaranteed fire — in the supervised native regime if needed — as an **upper-limit sanity check** that the BWT/FM readers react correctly; re-examine the drift verdict on a real fire. | ✅ **Complete** (an *upper-limit* supervised check: MAE **resists** forgetting even on CIFAR-100 — the key architectural finding — and a guaranteed fire comes only from a pretrained ViT-B/16 fine-tuned supervised (PC fires, healthy holds, `cka_drift` rehabilitated). *Not* the instrument+MAE target → P0.3.6) |
| P0.3.5 | [Forgetting-fire audit — genuine? permanent?](P0.3.5.md) | Motivation: the P0.2.4 analogue, now meaningful once a real fire exists (P0.3.4) — but an *audit* of that fire, not a constructed quiet-side calibration. Ask whether a fired BWT over-reads along two *orthogonal* axes: a kNN cross-probe (genuine vs apparent — real degradation or linear-probe artifact) and a recoverability re-learn (permanent vs recoverable — granting it's real, does it heal). Key framing: *recoverable ≠ fake*. | ✅ **Complete** (the fire is *genuine* — the kNN cross-probe craters deeper, so the refit-linear gate is conservative — and *severity ≠ permanence*: the operating-point fire fully recovers via a matched-LR task-A re-learn. An *audit*, not a constructed calibration — low-value for the refit-BWT (immune by construction), but a real calibration of the label-free MAE readers stays open future work) |
| P0.3.6 | [Positive control (forgetting gate) — the SSL-MAE fire (label-free)](P0.3.6.md) | Motivation: P0.3.4's fire is *supervised*; the calibration cafl4ds actually needs is **instrument + MAE, label-free**. Reuse the pretrained-deep-well lever with a **pretrained MAE** backbone continuing MAE training (no labels) on a cross-distribution phase B, and test whether an MAE backbone can be driven to a genuine crater *and* whether the label-free monitors (`cka_drift`, MAE recon loss) — the only signals available in deployment — track it. Its pass is what flips the forgetting **mode** to ✅. | ✅ **Complete** (the label-free MAE fire: cross-dataset Imagenette→CIFAR with matched Imagenette replay fires PC BWT −0.062, healthy holds −0.003, 18.5× contrast, monotone in LR; the **MAE recon gap** corroborates, `cka_drift` does *not* transfer from the supervised fire) |
| P0.3.7 | [Positive control (forgetting gate) — savings audit of the label-free fire](P0.3.7.md) | Motivation: the P0.3.5 analogue for the *label-free MAE* fire — a genuinely distinct experiment (in MAE the objective is decoupled from the probe readout, so "apparent forgetting" is more likely than in the supervised case). Use the **savings paradigm** — truly forgotten ⇒ relearning is as slow as from scratch — to test whether the gate-passing BWT crater is genuine loss or recoverable interference. | ✅ **Complete** (the savings test *could not be run* on this fire — the P0.3.6 crater is too shallow for a relearning-speed gap and the from-scratch pole is the wrong control, so the savings fraction is degenerate. What holds is a weak retention check: the forgotten encoder recovers task A like the held one, far above a from-scratch MAE — consistent with "MAE resists catastrophic forgetting," but near-tautological for a −0.062 crater. Whether a genuinely *deep* MAE crater is permanent or recoverable stays untested → P0.3.8 / Phase-1) |
| P0.3.8 | [Positive control (forgetting gate) — hard-disjoint-distribution vehicle](P0.3.8.md) | Motivation: P0.3.7 left the savings guard's *genuine* (savings≈0) pole unpopulated in the MAE regime, and scoped the resistance finding to short/near-IID/task-swap conditions. Try one cheap, in-regime *controlled vehicle* toward the genuine pole — a **hard-disjoint** phase-B distribution (structure-destroyed, e.g. phase-scrambled) that disrupts representation-level structure — and, as a nearly-free rider, a **cosmetic** grayscale shift to test whether the recon-loss reader *false-fires* on an output shift the representation shrugs off. | ✅ **Complete** (the hard-disjoint phase-scramble arm did *not* deepen the crater despite destroying spatial structure, and a *harder* task B that learns less → MAE's resistance strengthens on a structure-destroying shift. The genuine pole stays *untested*, not certified → Phase-1. The grayscale rider confirms a **recon-loss false positive** — recon gap ~10× the fire while probe BWT holds at baseline, a decoder-output shift not representation forgetting) |
| P0.4.0 | [Positive control (instability gate) — LR-driven divergence](P0.4.0.md) | Motivation: two-sided calibration of the **gradient-norm** instrument on the *divergence* pole, using LR as the clean forcing lever — grad norm must blow up (with / ahead of the loss going non-finite) under a pathological LR and stay bounded on a genuinely-learning healthy MAE. Plus a leading-indicator read (does the grad-norm spike *precede* the loss NaN?) and a collapse-specificity control (grad norm must **not** fire on the P0.2 collapse pole). MAE vehicle: its unbounded MSE loss can actually diverge, where SimSiam's bounded cosine loss collapses instead. | ✅ **Complete** (the two-sided grad-norm gate passes: healthy stays finite & learns, PC diverges; dose-response threshold `lr=3`, monotone, and — the payoff — **5 steps of lead time** before the loss NaN; specific to divergence, batch-robust, reproduced across seeds. Vehicle refined to MAE **+ SGD** — AdamW's normalized step damps the runaway to a no-lead knife-edge) |
| P0.4.1 | [Positive control (instability gate) — data surprise / shock](P0.4.1.md) | Motivation: the deployment-relevant driver — data *surprise* (OOD / shift bursts in the stream), not hyperparameters. Calibrate grad norm as a transient shock / change-point detector and separate a recoverable spike from a runaway (the P0.3 *severity ≠ permanence* analogue). Toy-scale caveat: surprise magnitude depends on base-distribution competence. Full correlated closed-loop dynamics → Phase-1. | ✅ **Complete** (the grad norm reads a *content* shock, **not** distribution shift: a saturating out-of-range noise burst fires two-sided then *recovers* (transient), and `burst_scale` is a fixed-LR severity dose-response with a transient→runaway threshold and lead time shrinking with severity. In-range distributional OOD — CIFAR / grayscale / **phase-scramble** — does *not* fire (recon difficulty ≠ novelty); LR-divergence is shock-independent. Vehicle: raw MSE, `norm_pix` off) |

## Artifacts

Each `scripts/positive_control.py` run writes a full `comparison.json` — `{gate, envelope, pc, healthy}`: the gate
verdict, the collapse-suite envelope, and both arms' full per-checkpoint health series — into the **Hydra run dir**
(`outputs/<date>/<time>/`, **gitignored and ephemeral**), so fresh runs are never tracked. The forgetting harness
`scripts/positive_control_forgetting.py` (P0.3) writes an analogous `comparison.json` = `{gate, pc, healthy}` (no
collapse envelope; each arm is a single two-phase summary record — accuracy matrix, BWT/FM, drift, recon). The
instability harness `scripts/positive_control_instability.py` (P0.4.0) writes `comparison.json` =
`{gate, lead_time, healthy, sweep}` — the gate verdict (two-sided contrast + dose-response + divergence), the lead-time
block, and per-arm summaries (steady grad-norm peak/median, first non-finite step, init/final loss); the bulky per-step
`{step, loss, grad_norm, finite}` traces that feed the console sparklines are dropped from the tracked artifact. The
data-surprise harness `scripts/positive_control_data_surprise.py` (P0.4.1) writes `comparison.json` =
`{gate, ladder, op_traces}` — the two-sided change-point gate at `lr_op`, the per-LR `ladder` (shock vs. control
summaries + any lead time), and the `op_traces` per-step series; the traces are likewise dropped from the tracked
artifact.

The runs that **back a substudy's cited numbers** are promoted, tracked, under `artifacts/<substudy-id>/`. The tracked
artifact is the **`{run, gate, envelope}` summary** — the gate verdict and the per-`(instrument × surface)` separations
that every table and post-hoc analysis actually reads, plus a `run` label identifying the config. The bulky
per-checkpoint `pc`/`healthy` series is dropped (it feeds only the console sparklines, which no table cites) to keep the
tree lean as substudies accumulate; re-run to regenerate it. Promoted artifacts are period-accurate — a file's gate
schema reflects the harness at the time of the run and is not retrofitted.

Present:

- [`artifacts/P0.2.2/`](artifacts/P0.2.2/) — all 88 E1–E4 runs (`E{1..4}/<run-name>.json`); the full set the E1–E4
    tables (min-across-4-seeds) and the E7/E8 pooled analyses rest on.
- [`artifacts/P0.2.3/`](artifacts/P0.2.3/) — the four redundancy-collapse sweep runs (`seed_N.json`).
- [`artifacts/P0.2.4/`](artifacts/P0.2.4/) — the 12 false-positive-stress runs (`<stressor>/seed_N.json` for
    `low_diversity`, `heavy_aug`, `undertrained`); the quiet-verdict + envelope behind the FP-boundary map. The Option B
    ablation adds [`artifacts/P0.2.4/collapse_pole/`](artifacts/P0.2.4/collapse_pole/) — the same 12 cells with a
    matched forced-collapse arm per regime (`{envelope, pole_envelope, positions}`: Option-A fire, in-regime
    discrimination, and the `[collapse, healthy]`-axis position behind the artifact-vs-blind-spot split).
- [`artifacts/P0.3.0/`](artifacts/P0.3.0/) — the canonical **forgetting** PC `comparison.json` (`{gate, pc, healthy}`)
    on the vehicles/animals distinct-task split (escalated MAE, HPU): the passing gate plus both two-phase arms behind
    the P0.3.0 BWT/FM contrast, drift-demotion, and recon-corroboration readings.
- [`artifacts/P0.3.1/`](artifacts/P0.3.1/) — the canonical **passing** forgetting PC `comparison.json` from the
    *erosion* corner of the operating-point map (256/8, `epochs_b=900`): the reference the map is read against (BWT
    −0.067, healthy +0.013), and — because both arms train the same 1200 epochs — the training-amount-controlled arm
    behind the P0.3.1 cka-drift verdict.
- [`artifacts/P0.3.4/`](artifacts/P0.3.4/) — the canonical **robust** forgetting fire on Split-CIFAR-100 via a
    pretrained ViT-B/16 backbone: the **N=5** `comparison.json`s (`n5_lr1e-4_seed{0..4}` — 4/5 PC fire, 5/5 healthy
    hold, the `cka_drift` separation) plus the seed-0 LR **dose-response** (`doseresp_lr{3e-5..3e-4}` — the monotonic
    crater curve behind the operating-point choice).
- [`artifacts/P0.3.5/`](artifacts/P0.3.5/) — the fire-audit reads on that fire: the **kNN cross-probe** N=5
    (`knn_crossprobe_seed{0..4}` — kNN PC BWT −0.22…−0.26 vs linear −0.06, the genuine-not-artifact evidence) and the
    **recoverability** depth map (`recovery_matched-lr{1e-4,2e-4,3e-4}` — recovery LR tied to each arm's phase-B LR, a
    fixed 256-step task-A re-learn budget; each `pc.recovery_curve` the trajectory: a true plateau back to R00 at the
    operating point, an incomplete-recovery gradient at the deeper craters — still creeping/noisy at step 256, so read
    against the step-budget caveat in P0.3.5.md).
- [`artifacts/P0.3.6/`](artifacts/P0.3.6/) — the **label-free SSL-MAE** cross-dataset fire: the seed-0 phase-B LR
    **dose-response** `comparison.json`s (`comparison_lr{1e-4,2e-4,3e-4,4e-4}_seed0` — `{gate, pc, healthy}`, the
    passing gate at `lr=3e-4` plus both two-phase arms behind the BWT/FM contrast, the MAE recon-gap corroboration, and
    the tiny/unordered `cka_drift` that fails to transfer from the supervised fire).
- [`artifacts/P0.3.7/`](artifacts/P0.3.7/) — the **savings audit** of that fire (`comparison_savings_lr3e-4_seed0`): the
    passing gate plus a `savings` block — per-init (`scratch` / `pc_forgot` / `healthy_held`) task-A relearn `curves` of
    `{step, acc, recon}` + `summary`, the trajectories behind the recoverable-under-a-matched-budget verdict (the
    savings fraction 1.0 is a degenerate retention indicator here, not a relearning-speed measure).
- [`artifacts/P0.3.8/`](artifacts/P0.3.8/) — the **hard-disjoint-distribution** probes
    (`comparison_{scrambled,grayscale}_lr3e-4_seed0`): arm S (phase-scrambled CIFAR) — the two-sided fire + `savings`
    block behind the crater-did-not-deepen verdict (crater depth −0.063 ≈ color baseline; savings 1.0 degenerate, not
    the verdict); arm G (grayscale CIFAR) — the recon-loss **false positive** (`recon_forget_gap` +0.301 with probe BWT
    at baseline).
- [`artifacts/P0.4.0/`](artifacts/P0.4.0/) — the **LR-divergence** dose-response `comparison.json`s (slimmed to
    `{gate, lead_time, healthy, sweep}`): `dose_seed0` + `dose_seed1` (the passing two-sided gate — `1.2×10⁶×` /
    `3.3×10⁵×` grad-norm contrast, threshold `lr=3`, the 5-step lead) and `batch16_seed0` (the batch-size-robustness
    ablation — same threshold + lead at `batch_size=16`). Cross-mode specificity reads off the (now
    grad-norm-instrumented) collapse harness and is not a separate artifact.
- [`artifacts/P0.4.1/`](artifacts/P0.4.1/) — the **data-surprise** reads (slimmed to `{gate, ladder}`): `gate_seed0` +
    `gate_seed1` (the passing two-sided change-point gate — saturating-noise shock, burst grad-norm `78× / 74×` the
    control's same steps, ~30× loss, transient recovery), `dataset_scrambled` (the distributional-OOD **non-fire** —
    phase-scrambled CIFAR at surprise `0.54×` / fire `1.2×`, the refutation), and `scale_dose/dose.json` (the
    `burst_scale` amplitude **severity dose-response** — transient→runaway threshold between scale 4 and 8, lead time
    `14 → 5`).
- **P0.2.1** has no directory of its own: its headline calibration corner (IID × 40 epochs) is the *same* corner as
    P0.2.2's [`E1/iid_40ep_seed*`](artifacts/P0.2.2/E1/) (same config, RankMe re-read — P0.2.2 cross-checks the
    reference did not move), so it is re-backed there. Its LR / horizon *ablation* runs were exploratory and not
    preserved as clean per-run artifacts; those tables are reproducible via the P0.2.1 "How to run" commands.
- **P0.2** predates the `comparison.json` writer, so no artifact exists; its one live conclusion (RankMe did not
    separate at the toy single-pass horizon) is subsumed by P0.2.1's regime finding.
